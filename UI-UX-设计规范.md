# 味禾小馆官网 · UI/UX 设计规范

> 版本：v1.0  
> 依据：PRD v1.4 + 高保真原型代码（`weihe-website/`） + `/ui-ux-pro-max` 设计原则  
> 技术栈：HTML5 + CSS3 + 原生 JavaScript（零构建、纯静态）

---

## 1. 项目概述

### 1.1 产品定位
味禾小馆官网是面向餐饮顾客、潜在加盟商、求职者及媒体/合作伙伴的**品牌展示型网站**，核心目标是传递"家常、新鲜、现炒、可信"的品牌价值，并承载招商与招聘入口。

### 1.2 目标用户与诉求

| 用户类型 | 核心诉求 | 关键页面 |
|---|---|---|
| 餐饮顾客 | 看菜品、找门店、了解品牌 | 首页、菜品中心、门店案例、关于我们 |
| 潜在加盟商 | 评估加盟可行性、发起咨询 | 加盟合作 → 联系我们 |
| 求职者 | 查看岗位、投递简历 | 人才招聘 → 联系我们 |
| 媒体/合作伙伴 | 获取品牌资料与联系方式 | 新闻、品牌简介、联系我们 |

### 1.3 设计语言关键词
- **温暖**：暖陶红 + 琥珀 + 奶油底，圆角卡片，柔和阴影。
- **家常**：亲切的中文无衬线字体、清晰的图文层级、真实的菜品/门店图片占位规范。
- **可信**：统一的联系方式入口、合规入口（隐私政策）、稳定的导航结构。
- **食欲**：菜品大图、价格与标签突出、暖色渐变点缀。

---

## 2. 设计原则

### 2.1 温暖感
- 背景使用奶油色（`#FBF6EE`），避免纯白带来的冷感。
- 品牌色为暖陶红（`#C8482E`），用于主按钮、重点强调、当前导航状态。
- 卡片使用 14px 圆角 + 柔和阴影，模拟"盛装菜肴的器皿"的圆润感。

### 2.2 可信感
- 所有页面共享统一的顶部导航与页脚，降低迷失感。
- 联系方式、加盟政策、隐私政策等合规入口在页脚与联系我们页明确呈现。
- 关键数字（如"全国 85 家直营门店"）全站口径一致。

### 2.3 清晰易用
- 核心任务（找菜 / 找店 / 加盟 / 应聘）均可在 **≤3 次点击**内到达。
- 一级导航仅 5 个，二级菜单悬停展开，移动端汉堡菜单收纳。
- 页面头部使用面包屑（`page-hero .crumb`）明确当前位置。

### 2.4 食欲导向
- 菜品与门店图片使用 4:3 / 16:10 / 16:9 等常见图片比例，便于后续替换真实摄影。
- 菜品卡片突出价格与标签（招牌/热销/粤味），引导决策。

---

## 3. 设计系统（Design System）

### 3.1 颜色系统

CSS 变量定义于 `assets/style.css` 的 `:root`。

| 变量名 | 色值 | 用途 |
|---|---|---|
| `--brand` | `#C8482E` | 品牌主色：主按钮、强调文字、当前导航、价格、时间线节点 |
| `--brand-dark` | `#A23A24` | 品牌深色：主按钮悬停 |
| `--amber` | `#E0A23B` | 辅助强调：CTA 横幅、热销标签、品牌徽标渐变 |
| `--fresh` | `#6E8B5B` | 新鲜/健康语义：招牌/每日标签、素菜相关 |
| `--cream` | `#FBF6EE` | 页面背景色 |
| `--surface` | `#FFFFFF` | 卡片、弹层、移动导航背景 |
| `--ink` | `#2E2A26` | 主文字颜色 |
| `--muted` | `#8A7E72` | 次要文字、说明文案 |
| `--line` | `#ECE3D6` | 边框、分割线 |

**语义色使用规范：**
- 正文、标题：使用 `--ink`。
- 说明、日期、占位提示：使用 `--muted`。
- 成功/健康/招牌：`--fresh`（低饱和绿，避免与"涨/跌"金融语义冲突）。
- 警示/热销：`--amber`（暖琥珀，餐饮场景友好）。
- 主行动点：`--brand`（陶红，食欲感强）。

### 3.2 字体系统

**字体栈（Font Stack）**
```css
font-family: "PingFang SC", "Microsoft YaHei", "Hiragino Sans GB", "Source Han Sans SC", sans-serif;
```
- 中文场景优先使用系统字体，保证加载速度与可读性。
- 未引入外部 Web 字体，避免阻塞渲染。

**字号层级**

| 层级 | 尺寸 | 用途 | 字重 |
|---|---|---|---|
| Hero 标题 | `46px`（移动端 `30–36px`） | 首页 Slogan | 700 |
| 页面标题 H1 | `34px` | 内页 `page-hero` 标题 | 700 |
| 区块标题 H2 | `30px` | `section-head h2` | 700 |
| 页面内二级标题 | `22px` | 隐私政策、长文章节 | 700 |
| 卡片标题 H3 | `17–18px` | 卡片、菜品名、步骤标题 | 700 |
| 正文 | `15px` | 段落、描述 | 400 |
| 小字 | `13–14px` | 日期、标签、脚注 | 400/600 |

**行高**
- 正文：`line-height: 1.7`
- 大标题：`line-height: 1.25`
- 区块副标题：`max-width: 620px`，居中对齐，保证每行阅读舒适。

### 3.3 间距系统

**基于 8px 基线**

| 变量/值 | 说明 |
|---|---|
| `padding: 0 20px` | 容器水平内边距 |
| `--maxw: 1160px` | 内容最大宽度 |
| `.section { padding: 72px 0 }` | 区块上下间距 |
| `gap: 22px` | 网格卡片间距 |
| `gap: 14px` | 按钮组、标签组间距 |
| `margin-bottom: 18px` | 表单字段、卡片内部间距 |

### 3.4 圆角与阴影

| 元素 | 圆角 | 阴影 |
|---|---|---|
| 卡片/菜品卡片 | `--radius: 14px` | `var(--shadow)` |
| 按钮 | `999px`（胶囊形） | 无 |
| 图片占位 | `12px` | 无 |
| 输入框 | `10px` | 无 |
| 子菜单 | `12px` | `var(--shadow)` |
| Hero 装饰图 | `24px` | `var(--shadow)` |
| CTA 横幅 | `20px` | 无（自身为渐变背景） |

**阴影定义**
```css
--shadow: 0 10px 30px rgba(120, 80, 40, 0.10);
```
- 暖棕色阴影，与品牌调性一致，避免冷灰色阴影。

### 3.5 网格系统

使用 CSS Grid，类名定义：

| 类名 | 桌面列数 | 900px 断点 | 720px 断点 |
|---|---|---|---|
| `.grid-2` | 2 列 | 2 列 | 1 列 |
| `.grid-3` | 3 列 | 2 列 | 1 列 |
| `.grid-4` | 4 列 | 2 列 | 1 列 |

- 网格间距统一为 `22px`。
- 卡片内部padding `26px`。

### 3.6 响应式断点

| 断点 | 目标设备 | 主要变化 |
|---|---|---|
| `> 900px` | 桌面 / 平板横屏 | 完整导航、多列网格、Hero 右侧装饰图显示 |
| `≤ 900px` | 平板竖屏 | `.grid-3/.grid-4` 降为 2 列；Hero 装饰图隐藏；Footer 单列；新闻卡片图片与文字上下堆叠 |
| `≤ 720px` | 手机 | 显示汉堡菜单；所有网格降为 1 列；Hero 标题缩小；CTA 横幅内边距减小；招聘岗位竖排 |

---

## 4. 组件库

### 4.1 顶部导航（`site-header`）

**结构**
```
.site-header
  └── .container.nav-inner
        ├── .brand（Logo + 品牌名）
        ├── .nav-toggle（移动端菜单按钮）
        └── nav.main-nav
              └── ul
                    ├── li > a（一级链接）
                    └── li.has-sub
                          ├── a（一级链接）
                          └── ul.submenu
                                └── li > a（二级链接）
```

**样式规范**
- 高度：`68px`。
- 背景：`rgba(251, 246, 238, 0.92)` + `backdrop-filter: blur(8px)`，滚动时保持可读。
- 固定定位：`position: sticky; top: 0; z-index: 50`。
- Logo：38×38px 圆角方块，渐变背景（`--brand` 到 `--amber`）。
- 一级链接：`15px`，内边距 `10px 14px`，圆角 `10px`，悬停背景 `--line`。
- 当前页：`li.active > a` 使用 `--brand` 色 + `font-weight: 700`。
- 二级菜单：最小宽度 `168px`，圆角 `12px`，阴影 `var(--shadow)`，默认透明隐藏，悬停/焦点时显示。
- 移动端（`≤720px`）：`.nav-toggle` 显示，`.main-nav` 为下拉面板，子菜单改为静态展开。

**交互**
- 桌面：鼠标悬停或键盘焦点（`focus-within`）展开二级菜单。
- 移动：点击 `.nav-toggle` 切换 `.open` 类；点击菜单链接后自动收起。

### 4.2 按钮（`.btn`）

| 类型 | 类名 | 背景 | 文字 | 边框 | 悬停 |
|---|---|---|---|---|---|
| 主按钮 | `.btn-primary` | `--brand` | 白色 | 透明 | `--brand-dark` |
| 幽灵按钮 | `.btn-ghost` | 透明 | `--brand` | `--brand` | 背景 `--brand`，文字白色 |
| 琥珀按钮 | `.btn-amber` | `--amber` | 白色 | 透明 | `brightness(0.95)` |

**统一规范**
- 内边距：`11px 22px`
- 圆角：`999px`（胶囊）
- 字号：`15px`，字重 `600`
- 过渡：`transition: .2s`

**使用场景**
- 主按钮：Hero CTA、表单提交、关键转化。
- 幽灵按钮：次要入口、卡片底部"查看更多"。
- 琥珀按钮：CTA 横幅内的强调按钮（与红色渐变背景形成对比）。

### 4.3 卡片（`.card`）

**规范**
- 背景：`--surface`（白色）
- 边框：`1px solid var(--line)`
- 圆角：`14px`
- 内边距：`26px`
- 阴影：`var(--shadow)`
- 悬停：`transform: translateY(-4px)` + 阴影保持

**内部元素**
- `.ico`：图标区，当前使用 emoji（🌿🔥🚚💰 等），**建议上线前替换为统一 SVG 图标集**。
- `h3`：标题，`18px`。
- `p`：描述，`14.5px`，`--muted`。

### 4.4 菜品卡片（`.dish`）

**结构**
```
.dish
  ├── .ph.ph-dish（菜品图片占位，aspect-ratio: 4/3）
  └── .body
        ├── h3（菜名）
        ├── p（简介）
        └── .meta
              ├── .badge（标签）
              └── .price（价格）
```

**规范**
- 图片区域：`aspect-ratio: 4/3`，圆角 `12px`。
- 内容区内边距：`16px 18px 20px`。
- 标签：`.badge`，胶囊形，`12.5px` 字重 `600`。
- 价格：`--brand` 色，字重 `700`。
- 分类属性：`data-cat="hot|soup|stir|staple"`，用于客户端筛选。

### 4.5 标签（`.badge`）

| 类名 | 背景 | 文字 | 语义 |
|---|---|---|---|
| `.badge-fresh` | `rgba(110,139,91,.14)` | `--fresh` | 招牌 / 每日 / 现做 / 健康 |
| `.badge-amber` | `rgba(224,162,59,.16)` | `#B9802A` | 热销 / 素菜 / 点心 |
| `.badge-brand` | `rgba(200,72,46,.12)` | `--brand` | 粤味 / 主食 / 品牌相关 |

### 4.6 图片占位（`.ph`）

用于上线前占位，渐变背景（`#D98B5F` → `#E0A23B`），白色文字。

| 类名 | 比例 | 用途 |
|---|---|---|
| `.ph-dish` | 4:3 | 菜品图片 |
| `.ph-store` | 16:10 | 门店实景 |
| `.ph-news` | 16:9 | 新闻配图 |
| `.ph-hero` | 16:7 | 大幅 Hero 配图 |
| `.ph-tall` | 3:4 | 竖版配图（品牌故事） |

**上线替换规范**
- 真实图片应保持与占位相同的 `aspect-ratio`，避免布局跳动。
- 建议使用 `object-fit: cover`。
- 所有图片必须提供描述性 `alt` 文本。

### 4.7 表单字段（`.field`）

**结构**
```
.field
  ├── label（标签）
  └── input / textarea
```

**规范**
- 标签：`14px`，`--ink`，底部间距 `6px`。
- 输入框：宽度 `100%`，内边距 `11px 14px`，圆角 `10px`，边框 `1px solid var(--line)`。
- 聚焦：`border-color: var(--brand)`，无 outline。
- 当前为演示表单，未接后端。

### 4.8 时间线（`.timeline`）

用于发展历程页面。
- 左侧竖线：`2px`，`--line`。
- 节点：`14px` 圆点，`--brand` 填充，白色边框 + 品牌色描边。
- 年份文字：`--brand`，`18px`，字重 `800`。

### 4.9 步骤（`.step`）

用于加盟流程页面。
- 编号圆圈：`40×40px`，`--brand` 背景，白色数字。
- 步骤标题：`16px`。
- 步骤描述：`14.5px`，`--muted`。
- 分隔：底部 `1px dashed var(--line)`。

### 4.10 招聘条目（`.job`）

- 背景白色、边框、圆角 `14px`、内边距 `20px 22px`。
- Flex 布局：左侧岗位信息 + 右侧"投递"按钮。
- 移动端（`≤720px`）：改为竖排，按钮与文字左对齐。

### 4.11 CTA 横幅（`.cta-band`）

- 背景：线性渐变（`--brand` → `--brand-dark`）
- 文字：白色
- 圆角：`20px`
- 内边距：`48px`（移动端 `34px 22px`）
- 内部可并列主按钮与幽灵按钮（幽灵按钮需覆盖为白色边框/文字）。

### 4.12 页脚（`site-footer`）

- 背景：`#2E2A26`（深棕，与 `--ink` 一致）
- 文字主色：`#E9E2D8`
- 链接悬停：`--amber`
- 底部栏：`#9A9085`，`13px`
- 包含：品牌名、Slogan、联系方式、快速链接、隐私政策链接、版权声明。

---

## 5. 全局交互规范

### 5.1 导航交互
- **桌面**：一级链接悬停背景变浅；有二级菜单的项悬停/聚焦时展开子菜单，动画 `0.18s`。
- **移动**：点击汉堡按钮展开全宽导航面板；二级菜单默认展开；点击任意链接后自动收起。
- **当前状态**：`li.active > a` 使用品牌色加粗，帮助用户定位。

### 5.2 链接与按钮状态
- 默认：继承颜色或品牌色。
- 悬停：颜色变为 `--brand` 或背景变化。
- 焦点：浏览器默认焦点环可见（建议后续统一为 `outline: 2px solid var(--brand); outline-offset: 2px`）。
- 激活：按钮 `brightness` 或背景加深。

### 5.3 卡片悬停
- `transform: translateY(-4px)`，过渡 `0.2s`。
- 避免使用导致布局位移的缩放（`scale`）。

### 5.4 菜品分类筛选
- 筛选条（`.cat-filter`）位于页面标题下方。
- 点击筛选按钮时：
  1. 移除所有 `.on` 类；
  2. 为当前按钮添加 `.on`；
  3. 读取 `data-filter`；
  4. 遍历 `.dish`，匹配 `data-cat`，不匹配项 `display: none`。
- 选择"全部"（`data-filter="all"`）时显示所有菜品。
- 无整页刷新，满足 PRD AC-2。

### 5.5 表单提交（演示阶段）
- 当前 `<form onsubmit="return false">`，提交无实际动作。
- 后续量产方案：接入第三方表单服务或自建轻量 API，并在提交后给出成功反馈（Toast 或按钮状态变化）。

---

## 6. 页面级 UI/UX 规格

### 6.1 首页（`index.html`）

**结构**
1. Hero 区：Slogan + 标签 + 双 CTA + 右侧装饰图。
2. 品牌优势：4 列卡片（每日鲜采、大火现炒、冷链品控、家常定价）。
3. 招牌推荐：4 列菜品卡片 + "进入菜品中心"。
4. 门店网络：3 列门店卡片 + "查看门店案例"。
5. 加盟 CTA：渐变横幅 + 两个按钮。
6. 品牌动态：2 条新闻摘要 + "更多企业新闻"。
7. 页脚。

**UX 要点**
- Hero 区双 CTA 分别指向顾客路径（菜品）与加盟商路径（加盟）。
- 卡片图标当前为 emoji，**建议替换为 SVG 图标**。
- 所有"查看更多"按钮均使用 `.btn-ghost` 幽灵按钮，降低视觉竞争。

### 6.2 菜品中心（`products/dishes.html`）

**结构**
1. 页面标题区（面包屑 + H1 + 说明）。
2. 分类筛选条（全部 / 招牌热菜 / 精致靓汤 / 家常小炒 / 主食点心）。
3. 4 列菜品网格（含 `data-cat` 属性）。
4. 页脚。

**UX 要点**
- 筛选条默认"全部"高亮。
- 点击筛选后实时过滤，无页面刷新。
- 菜品图占位比例 4:3，后续替换真实摄影时保持比例。

### 6.3 门店案例展示（`products/stores.html`）

**结构**
1. 页面标题区。
2. 门店卡片网格（3 列）。
3. 页脚。

**UX 要点**
- 门店图占位比例 16:10。
- 每张卡片包含城市+店名 + 一句话亮点。

### 6.4 企业新闻（`news/corporate.html`）与行业资讯（`news/industry.html`）

**结构**
1. 页面标题区。
2. 文章列表（左图右文，`.post`）。
3. 页脚。

**UX 要点**
- 左侧图片占位 16:9。
- 文章包含日期、标题、摘要。
- 移动端图片与文字上下堆叠。

### 6.5 加盟合作（`franchise/cooperation.html`）

**结构**
1. 页面标题区。
2. 四大支持体系卡片（4 列）。
3. 加盟流程步骤（5 步）。
4. CTA 横幅（联系招商顾问 / 查看招聘岗位）。
5. 页脚。

**UX 要点**
- 当前为占位政策内容，**上线前必须补充**：特许经营备案号、加盟资质、投资风险提示（PRD §9 合规要求）。
- 步骤编号清晰，流程可视化降低决策门槛。

### 6.6 人才招聘（`franchise/jobs.html`）

**结构**
1. 页面标题区。
2. 岗位列表（2 列 `.job` 卡片）。
3. 福利待遇说明卡。
4. 页脚。

**UX 要点**
- 每个岗位右侧"投递"按钮指向联系我们页。
- 岗位信息包含职位名与工作地点/类型。

### 6.7 品牌简介（`about/intro.html`）

**结构**
1. 页面标题区。
2. 品牌定位文案 + 标签。
3. 企业文化三卡（愿景 / 经营理念 / 核心价值观）。
4. 页脚。

**UX 要点**
- 文案突出"不用劣质食材，用心做好每一餐"等品牌承诺。

### 6.8 发展历程（`about/history.html`）

**结构**
1. 页面标题区。
2. 纵向时间线（2016 / 2018 / 2019 / 2022 / 2026）。
3. 页脚。

**UX 要点**
- 时间线从左到右自然阅读，节点突出。
- 年份与事件描述层级清晰。

### 6.9 品牌故事（`about/story.html`）

**结构**
1. 页面标题区。
2. 创始人叙事 + 配图占位。
3. 页脚。

**UX 要点**
- 采用长文叙事风格，段落宽松。
- 配图占位 `.ph-tall`（3:4），后续替换真实人物/门店老照片。
- 注意口径：正文中"80 余家"需与全站"85 家"统一或加注说明（PRD §4.3）。

### 6.10 联系我们（`about/contact.html`）

**结构**
1. 页面标题区。
2. 2 列布局：左侧联系卡片（招商 / 品牌媒体 / 总部地址），右侧在线留言表单。
3. 页脚。

**UX 要点**
- 表单字段包含称呼、联系方式、留言类型、留言内容。
- 当前为演示表单，未接后端。
- 占位联系方式需在上线前替换为真实信息。

### 6.11 隐私政策（`privacy.html`）

**结构**
1. 页面标题区（含"草稿，待法务审核"提示）。
2. 窄容器（`max-width: 820px`）长文，分六节：信息收集、使用、存储保护、用户权利、Cookie 与统计、联系我们。
3. 页脚。

**UX 要点**
- 从全站页脚统一入口进入。
- 标题使用 `22px`，正文使用 `--muted`，降低阅读压迫感。
- 顶部明确标注草稿状态，避免法律风险。

---

## 7. 响应式规则

### 7.1 桌面端（`> 900px`）
- 顶部导航完整展开，二级菜单悬停显示。
- Hero 右侧装饰图显示。
- 网格按设计列数排列（2/3/4 列）。
- Footer 三列布局。

### 7.2 平板端（`≤ 900px`）
- `.grid-3`、`.grid-4` 降为 2 列。
- Hero 装饰图隐藏，标题缩小至 `36px`。
- Footer 变为单列。
- 新闻卡片图片与文字上下堆叠。

### 7.3 移动端（`≤ 720px`）
- 显示汉堡菜单，导航面板下滑展开。
- 所有网格降为 1 列。
- Hero 标题 `30px`。
- CTA 横幅内边距减小。
- 招聘岗位卡片改为竖排。
- 正文最小字号保持 `15px`（建议后续调整为 `16px` 以提升移动端可读性）。

---

## 8. 动画与过渡

| 元素 | 属性 | 时长 | 说明 |
|---|---|---|---|
| 一级链接悬停 | `background`, `color` | `0.2s` | 背景变浅，文字变红 |
| 子菜单展开 | `opacity`, `visibility`, `transform` | `0.18s` | 向下平移 6px 淡入 |
| 卡片悬停 | `transform`, `box-shadow` | `0.2s` | 上移 4px |
| 按钮悬停 | `background`, `filter` | `0.2s` | 主按钮加深，琥珀按钮变暗 |
| 移动导航展开 | `max-height` | `0.25s` | 从 0 到 80vh |
| 筛选切换 | `display` | 即时 | 通过 JS 切换，无动画 |

**动画原则（来自 `/ui-ux-pro-max`）**
- 优先使用 `transform` 与 `opacity`，避免触发重排。
- 动画时长控制在 150–300ms 之间。
- 上线后应检查 `prefers-reduced-motion`，为减弱动画偏好用户提供无动画版本。

---

## 9. 可访问性（Accessibility）

### 9.1 颜色对比度
- 正文 `--ink`（`#2E2A26`）在 `--cream`（`#FBF6EE`）背景上，对比度充足，满足 WCAG 4.5:1。
- `--muted`（`#8A7E72`）用于次要文字，建议仅用于非关键说明；关键操作文字使用 `--ink` 或 `--brand`。

### 9.2 图片与多媒体
- 所有 `<img>` 必须提供描述性 `alt` 文本。
- 当前占位图使用 `.ph` div，上线后应替换为 `<img>` 并补 `alt`。

### 9.3 表单可访问性
- 每个 `input` / `textarea` 已关联 `<label>`。
- 建议后续为必填项添加 `required` 与 `aria-invalid` 状态。

### 9.4 键盘导航
- 导航已使用 `:focus-within` 支持键盘展开子菜单。
- 建议后续统一焦点环样式，并添加 skip-link 跳转至主内容。

### 9.5 语义化 HTML
- 已使用 `header`、`nav`、`main`、`section`、`footer`。
- 当前页面缺少 `<main>` 包裹主内容区，建议后续补充。

### 9.6 触摸目标
- 导航链接、按钮、筛选条、"投递"按钮均满足最小 44×44px 触摸目标。

---

## 10. 性能与图片规范

### 10.1 性能目标（来自 PRD §1.2）
- 首屏可交互（移动端 4G）：≤ 2.5s。
- 单页体积：≤ 300KB（不含图片）。
- Lighthouse 性能分：≥ 90。

### 10.2 当前实现优势
- 纯静态，无外部请求阻塞。
- 未引入 Web 字体，使用系统字体栈。
- 未引入大型 JS 框架。

### 10.3 图片规范
- 格式：WebP 优先，JPEG/PNG 兜底。
- 压缩：菜品/门店图片建议 ≤ 200KB/张。
- 尺寸：按占位比例输出（4:3、16:10、16:9）。
- 懒加载：首屏以下图片添加 `loading="lazy"`。
- `srcset`：针对 1x/2x 屏幕提供不同分辨率。

### 10.4 后续优化建议
- 添加 `favicon.ico`。
- 添加 Open Graph（OG）分享图。
- 生成 `sitemap.xml` 与 `robots.txt`。
- 接入轻量统计（百度统计 / Plausible / GA4）。

---

## 11. 附录

### 11.1 与 PRD 验收标准（AC）对照表

| PRD 验收标准 | UI/UX 规范对应 | 状态 |
|---|---|---|
| AC-1 导航（桌面悬停/移动汉堡） | §4.1、§5.1、§7 | 已实现 |
| AC-2 菜品中心实时筛选 | §4.4、§5.4 | 已实现 |
| AC-3 联系表单校验与反馈 | §4.7、§5.5 | 前端演示已实现，后端待接入 |
| AC-4 响应式（900px/720px） | §3.6、§7 | 已实现 |
| AC-5 SEO 基础 | §10.4 | 标题/description 已实现，OG/sitemap 待做 |
| AC-6 合规（ICP/备案/隐私政策） | §6.5、§6.11 | 隐私政策草稿已建，备案号待提供 |
| AC-7 链接完整性 | 全站导航与页脚 | 已实现 |

### 11.2 当前代码中的待优化项

| # | 问题 | 影响 | 建议 |
|---|---|---|---|
| 1 | 首页品牌优势卡片使用 emoji 图标（🌿🔥🚚💰） | 风格不统一、跨平台显示差异 | 替换为统一 SVG 图标集（如 Lucide / Heroicons） |
| 2 | 主内容区未使用 `<main>` 标签 | 可访问性 / 语义化不足 | 各页面用 `<main>` 包裹 Hero/section |
| 3 | 筛选切换为即时 `display` 切换，无过渡 | 体验略显生硬 | 可增加淡入淡出（需避免布局跳动） |
| 4 | 移动端正文字号为 `15px` | 部分设备可读性稍弱 | 建议调整为 `16px` |
| 5 | 表单提交无成功反馈 | 用户不确定是否提交 | 量产时添加 Toast 或按钮 loading 状态 |

### 11.3 文件结构对应

| 设计规范章节 | 对应代码文件 |
|---|---|
| 设计系统 | `assets/style.css`（`:root`、通用类） |
| 全局交互 | `assets/main.js` |
| 首页 | `index.html` |
| 产品页 | `products/dishes.html`、`products/stores.html` |
| 新闻页 | `news/corporate.html`、`news/industry.html` |
| 招商加盟页 | `franchise/cooperation.html`、`franchise/jobs.html` |
| 关于我们页 | `about/intro.html`、`about/history.html`、`about/story.html`、`about/contact.html` |
| 隐私政策 | `privacy.html` |
| 原型评审器 | `prototype.html` |
