# 味禾小馆官网后端依赖
# 安装：pip install -r requirements.txt

# Web 框架
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# 数据校验 / 配置
pydantic>=2.8.0
pydantic-settings>=2.4.0

# ORM / 迁移
sqlalchemy>=2.0.31
alembic>=1.13.0

# 数据库驱动：本地 SQLite 无需驱动；生产 PostgreSQL 使用 psycopg (v3)
psycopg[binary]>=3.1.0

# 鉴权 / 限流
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# 注意：passlib 1.7.4 与 bcrypt>=4.1 的后端检测不兼容（bcrypt 5.x 对 >72 字节测试密码抛错），
# 因此锁定 bcrypt 4.0.1 以保证默认管理员密码哈希可正常生成。
bcrypt==4.0.1
slowapi>=0.1.9

# 测试
pytest>=8.0.0
httpx>=0.27.0
