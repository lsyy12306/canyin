# 味禾小馆官网 · 项目开发实施方案

> 版本：v1.0（已确认并执行；阶段 0–5 已交付，阶段 6 留后续）  
> 日期：2026-08-26  
> 实施期调整：见 `README.md` §6 与 `开发技术文档.md` §14。  
> 约束：严格基于现有文档开发（实施期与原文档的偏离已同步回写至开发技术文档 v1.4）。

---

## 0. 方案依据（唯一事实来源）

本方案所有页面效果、数据结构、接口契约、设计变量均以已交付文档为准，不另起炉灶：

| 文档 | 版本 | 本方案引用内容 |
|---|---|---|
| `PRD-味禾小馆官网.md` | v1.4 | 信息架构（12 页）、目标用户、KPI/验收标准（AC）、合规阻断项、数字口径 |
| `UI-UX-设计规范.md` | v1.0 | 设计系统（颜色/字体/间距/圆角/阴影）、组件交互、响应式断点 |
| `开发技术文档.md` | v1.3 | 技术栈、目录结构、接口清单、前后端实现要点、测试/部署策略 |
| `数据库设计文档.md` | v1.1 | 10 张表结构、数据字典、建表 SQL、索引、约束（**数据库相关以本文档为准**） |
| `合规备案清单.md` | — | 上线阻断项（ICP/公安/特许经营备案、隐私政策、风险提示） |
| `weihe-website/*.html` + `assets/*.svg` | — | 静态高保真原型：页面效果参考与逐页对照蓝本 |

> ⚠️ 与原「开发技术文档」的两处必要调整（因本次明确要后台管理系统）：  
> ① 后台由「可选」升级为**必做**，启用 `users` 表与 `routers/admin.py`、JWT 鉴权；  
> ② 在原 `frontend/`（官网）之外**新增 `admin/`**（Ant Design 管理台），详见 §3。

---

## 1. 目标与范围

### 1.1 交付物（本次范围）

1. **官网前台**（React + Vite + Tailwind + React Router）：12 个页面，严格还原原型视觉与 PRD 信息架构。
2. **后台管理系统**（React + Vite + Ant Design）：登录 + 菜品/门店/新闻/岗位 CRUD + 加盟意向/简历/留言查阅 + 站点配置。
3. **后端服务**（FastAPI + SQLAlchemy + PostgreSQL）：10 张业务表的读写接口 + 表单提交（真实落库、限流）+ 管理后台鉴权。
4. **数据库**：PostgreSQL 14+，按数据库设计文档 v1.1 建表、迁移、种子数据。
5. **本地可运行联调环境**：前后端 + 数据库一键启动，本地预览。

### 1.2 不在本次范围（后续阶段）

- 生产部署（Docker Compose 多服务编排、CI/CD、Nginx 反代）——已与用户确认**留作后续独立阶段**。
- 真实备案号/证照（占位，待品牌方提供）。
- 邮件/短信通知（默认预留钩子，MVP 不启用，见 §6 假设）。

---

## 2. 已确认的关键决策（来自用户）

| # | 决策点 | 结论 |
|---|---|---|
| D1 | 项目目录 | 在现有 `weihe-website/` 内构建 `frontend/` + `backend/` + `admin/`，保留静态原型作参考 |
| D2 | 后台 UI | **Ant Design**（与官网 Tailwind 视觉解耦，开发最快） |
| D3 | 资源存储 | **本地磁盘** `backend/app/static/uploads/`，数据库存相对路径 |
| D4 | 交付范围 | **本地可运行版**，部署（Docker/CI-CD）留后续 |
| D5 | 后台管理系统 | **必做**（用户明确要求），`users` 表与 admin 接口启用 |

---

## 3. 工程结构（在 weihe-website/ 内）

```
weihe-website/
├── 项目开发实施方案.md            ← 本文件
├── PRD-味禾小馆官网.md
├── UI-UX-设计规范.md
├── 开发技术文档.md
├── 数据库设计文档.md
├── 合规备案清单.md
├── index.html / about/ / products/ / news/ / franchise/ / privacy.html   ← 静态原型（参考，不删除）
├── assets/ (style.css, main.js, *.svg)                                    ← 设计变量与图源（参考）
│
├── frontend/                     # 官网前台（React+Vite+Tailwind+React Router）
│   ├── index.html  vite.config.ts  tailwind.config.js  package.json
│   └── src/
│       ├── main.tsx  App.tsx  routes.tsx
│       ├── api/  components/  pages/  hooks/  types/  styles/  utils/
│
├── admin/                        # 后台管理系统（React+Vite+Ant Design）【新增】
│   ├── index.html  vite.config.ts  package.json
│   └── src/
│       ├── main.tsx  App.tsx  routes.tsx
│       ├── api/  components/  pages/  store/(auth)  utils/
│
└── backend/                      # FastAPI 后端
    ├── app/
    │   ├── main.py  config.py  database.py  security.py(JWT)  deps.py
    │   ├── models/  schemas/  routers/  services/  migrations/  static/uploads/
    │   └── tests/
    ├── alembic.ini  pyproject.toml  requirements.txt
    └── Dockerfile
```

> 后台 `admin/` 为本次新增；原开发技术文档仅含 `frontend/`。这是为落实 D5（后台必做）对目录结构的必要扩展，其余沿用技术文档 §4。

---

## 4. 阶段划分与里程碑（编码将在确认后按序执行）

### 阶段 0 — 环境与脚手架（P0）
- 技术基线：Node 20+、Python 3.11+、PostgreSQL 14+（本地或 `docker run postgres:15-alpine`）。
- 初始化 `frontend/`、`admin/`、`backend/` 三个工程的依赖与构建配置；Tailwind 注入 UI/UX §3 设计令牌；Alembic 初始化。
- 本地 `.env` 模板（`DATABASE_URL`、`SECRET_KEY`、`ALLOWED_ORIGINS`、`UPLOAD_DIR`）。
- **产出**：三个工程可 `install` 且能起空壳；DB 可连。

### 阶段 1 — 数据库层（P0）
- 依据 `数据库设计文档.md` v1.1 §4–§5 落地 10 张表 ORM 模型（`models/`），字段/约束/索引/外键严格一致（含 `config_key/config_value/config_group` 重命名、VARCHAR 长度、CHECK、`ip_address`）。
- Alembic 初始迁移脚本；**种子脚本**把静态原型中的示例菜品/门店/新闻/岗位 + `site_configs` 预置项（数据库文档 §5 种子数据）灌入，便于联调。
- **产出**：`alembic upgrade head` 建表成功，种子数据可见。

### 阶段 2 — 后端 API（P0）
- FastAPI 入口、CORS（允许 `frontend`/`admin` 开发域名）、健康检查、统一响应 `{code,message,data}`。
- 公共读接口：`/api/public/configs`、`/api/dishes`、`/api/dish-categories`、`/api/stores`、`/api/news`（严格按技术文档 §6.3）。
- 表单写接口：`/api/jobs/{id}/applications`、`/api/franchise/inquiries`、`/api/contact/messages`——真实落库、`slowapi` 限流（5/min/IP）、记录 `ip_address`。
- **后台必做**：`security.py`（JWT）、`routers/admin.py`——登录/刷新、菜品/门店/新闻/岗位 CRUD、`site_configs` 管理、加盟意向/简历/留言查阅列表；`users` 表启用，`role` 区分 admin/editor。
- **产出**：Swagger（`/docs`）可跑通全部接口；pytest 冒烟测试通过。

### 阶段 3 — 官网前台（P0）
- Vite + React + React Router，路由映射 PRD 12 页（技术文档 §8.1）。
- 设计系统：Tailwind `theme.extend` 注入 UI/UX §3 颜色/圆角/阴影/字体栈；`index.css` 落地 8px 间距基线、奶油底 `--cream` 等。
- 组件：Layout/Header/Footer、Hero、SectionHeader、DishCard、StoreCard、NewsCard、JobCard、Timeline、StepFlow、ContactForm、PlaceholderImage（对应原型与 UI/UX 组件交互）。
- 页面：首页、菜品中心（分类筛选，对应 AC-2）、门店案例、企业新闻/行业资讯、加盟合作、人才招聘、品牌简介/发展历程/品牌故事/联系我们、隐私政策（渲染 `site_configs.privacy_policy` Markdown）。
- 数据：axios 实例 + SWR hooks（`useDishes/useStores/useNews`）+ zod 表单校验 + 提交 Toast 反馈。
- SEO：Helmet 动态 title/description/OG；`robots.txt` + `sitemap.xml`。
- 响应式：按 UI/UX §3.6 断点（桌面悬停二级菜单 / ≤720px 汉堡菜单）。
- 合规：页脚动态读取 `site_configs`（ICP/公安/特许经营备案、风险提示、联系方式）。
- **产出**：12 页视觉与静态原型一致；表单真实提交成功。

### 阶段 4 — 后台管理系统（P0，依据 D2/D5）
- Vite + React + Ant Design；登录页（JWT 存储）；受保护路由 + 角色。
- 布局：Sider 导航 + Header；模块：菜品管理、门店管理、新闻管理、岗位管理（antd Table/Form/Upload 增删改查）；加盟意向、简历投递、在线留言（列表+状态流转）；站点配置（编辑备案号/隐私政策等）。
- 对接阶段 2 的 `/api/admin/*`；上传走本地磁盘（D3）。
- **产出**：管理员可登录并维护全部业务数据。

### 阶段 5 — 联调、本地预览与质量（P1）
- 本地联调：`frontend :5173`、`admin :5174`、`backend :8000`、PostgreSQL；CORS 放行。
- 功能核对：逐页对照原型 + PRD 验收标准（AC-1~AC-7）；合规清单逐项确认展示。
- 测试：后端 pytest；前端 vitest 组件测试；Playwright E2E 覆盖关键路径（首页→菜品筛选、加盟表单提交、招聘→联系）。
- 性能：单页 JS ≤300KB（gzipped，技术文档 §1.2）；Lighthouse 性能 ≥90（尽力，非阻断）。
- **产出**：本地一键预览可演示；测试通过；合规项可见。

### 阶段 6 — 生产部署（后续，非本次）
- Docker Compose 多服务、CI/CD、Nginx 反代（技术文档 §12）——按 D4 留待后续阶段。

---

## 5. 文档→代码的对应关系（保证「严格基于现有文档」）

| 维度 | 来源文档/章节 | 落地方式 |
|---|---|---|
| 页面与路由 | PRD §3（12 页）、技术文档 §8.1 | `frontend/src/routes.tsx` + `pages/` |
| 视觉与组件 | UI/UX §3 设计系统、原型 `*.html` | Tailwind 令牌 + `components/` |
| 颜色/字体/间距 | UI/UX §3.1–3.4、技术文档 §8.6 | `tailwind.config.js` + `index.css` |
| 数据表 | 数据库文档 v1.1 §4–§5 | `backend/app/models/` + Alembic |
| 接口契约 | 技术文档 §6 | `backend/app/routers/` + `frontend/src/api/` |
| 表单与限流 | 技术文档 §9.4、§10.4 | `slowapi` + `ip_address` 落库 |
| 合规 | PRD §9、合规备案清单、数据库文档 `site_configs` | 页脚/加盟页动态配置 |
| 数字口径 | PRD §4.3（85 家/2016 广州/人均55–75） | 种子数据与文案统一 |

---

## 6. 默认假设（可在确认时推翻）

1. **SEO 框架**：保持技术文档选定的 **Vite SPA（客户端渲染）** + Helmet + sitemap + robots；不引入 Next.js SSR（用户明确要 React，且 SPA 已满足基础 SEO）。
2. **通知（邮件/短信）**：MVP **默认不启用**，仅在表单提交处预留 `email_service` 异步钩子，后续接入。
3. **人机验证（reCAPTCHA/hCaptcha）**：MVP **默认不接入**；以接口限流（5/min/IP）+ `ip_address` 审计作为主防刷手段，代码预留开关。
4. **软删除**：遵循数据库文档 §1.3，**不引入** `is_deleted`，内容上下线用 `is_active`。
5. **门店数口径**：全站统一「85 家（2026）」（PRD §4.3）；品牌故事页历史叙述「80 余家」作为发展叙事保留，上线前与品牌方对齐（PRD 已标注）。
6. **合规占位**：ICP/公安/特许经营备案号与真实隐私政策正文为占位，待品牌方提供后再上线（合规备案清单列为 Launch Blocker）。
7. **种子图片**：先用占位图/示例图，不阻塞联调；真实摄影后续替换。

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| 前后台 UI 技术不同（Tailwind vs antd）导致视觉割裂 | 低（本就两类受众） | 仅后台用 antd；官网严格守 UI/UX 令牌 |
| 本地预览需同时起 3 个服务 + DB | 中 | 提供 `docker-compose`（仅 PG）+ 启动脚本/`Makefile` |
| 原型硬编码内容需转为种子数据 | 低 | 阶段 1 种子脚本一次性迁移 |
| 合规占位未填导致无法真实上线 | 高但非技术 | 已列为 Launch Blocker，仅影响部署阶段 |
| 门店数 85 vs 80 余口径 | 低 | 种子与文案统一为 85，叙事页加注 |

---

## 8. 验收对齐（PRD 验收标准）

- AC-1 导航（悬停二级/移动汉堡）→ 阶段 3 Header/Footer + 响应式。
- AC-2 菜品分类实时筛选 → 阶段 3 `DishListPage` + `/api/dishes?category=`。
- AC-3 联系/加盟/招聘表单真实留资 → 阶段 2 写接口 + 阶段 3 表单。
- AC-4 响应式 → UI/UX §3.6 断点。
- AC-5 SEO → 阶段 3 Helmet/sitemap/robots。
- AC-6 合规（备案号/隐私政策/风险提示配置化）→ `site_configs` + 页脚/加盟页。
- AC-7 链接完整性 → React Router + E2E 测试。

---

## 9. 实施记录（v1.0 → 执行）

**确认时间**：2026-08-26  
**沙箱约束**：本环境无外网，**未执行** `pip install` / `npm install` / 启动任何服务（DNS 不可达）。

**交付清单：**
- `frontend/`：React 18 + Vite + TS + Tailwind 官网前台，12 个页面 + 共享组件 + SWR/axios + zod + Helmet，58 个文件
- `admin/`：React 18 + Vite + TS + Ant Design v5 后台，登录 + 9 个管理模块（仪表盘/菜品/门店/新闻/岗位/加盟意向/简历/留言/站点配置），39 个文件
- `backend/`：FastAPI + SQLAlchemy 2.0（同步），10 张表 ORM、Alembic 初始迁移、种子脚本、JWT 鉴权、slowapi 限流、统一响应包络，45 个 .py
- `backend/.env.example`、`docker-compose.yml`（可选 PostgreSQL）、根 `README.md`

**已通过的校验：**
- 后端 45 个 Python 文件 `py_compile` 语法编译全部通过
- 路由清单抽取：所有接口路径与开发技术文档 §6 契约一致
- 集成期契约核对：修正后台状态流转路径（`/{id}/status`）与公共配置字段映射（`key/value`）

**未完成/不可执行项：**
- 端到端运行验证（`pip install` + 启动 uvicorn + 前台/后台联调）——需在联网本机按 `README.md` §2 执行
- 阶段 6 生产部署（Docker Compose 多服务编排 + CI/CD + Nginx）——按用户决策留后续

**与原方案/技术文档的偏离（已同步回写）：**
- 后台由"可选"升级为"必做"，并新增 `admin/` 目录
- 后端数据库驱动由 asyncpg 异步改为 psycopg / SQLite 同步，字段做方言兼容
- 状态流转路径统一为 `/{id}/status`；公共配置统一为 `key/value`

**待用户决定（上线前）：**
- 备案号（ICP / 公安 / 特许经营）由后台 → 站点配置 填入
- 默认管理员密码 `admin / admin123456` 必须修改
- 真实菜品牌照 / 门店实景图 / 隐私政策正文替换占位
- `SECRET_KEY` 在生产环境设为固定强随机串
